<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:SchoolProyectApp.ViewModels"
             xmlns:local="clr-namespace:SchoolProyectApp.Views"
             x:Class="SchoolProyectApp.Views.GradesPage"
             xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             xmlns:droid="clr-namespace:CommunityToolkit.Maui.PlatformConfiguration.AndroidSpecific;assembly=CommunityToolkit.Maui"
             droid:NavigationBar.Color="white"
             droid:NavigationBar.Style="LightContent"
             Title="Mis Calificaciones"
             BackgroundColor="{x:Static vm:ThemeColors.PageBackgroundColor}">

    <ContentPage.Behaviors>
        <toolkit:StatusBarBehavior StatusBarColor="{x:Static vm:ThemeColors.PrimaryColor}"
                                   StatusBarStyle="LightContent" />
    </ContentPage.Behaviors>

    <ContentPage.BindingContext>
        <vm:GradesViewModel />
    </ContentPage.BindingContext>

    <AbsoluteLayout>
        <ContentView x:Name="MainContent"
                     AbsoluteLayout.LayoutBounds="0,10,1,0.85"
                     AbsoluteLayout.LayoutFlags="WidthProportional,HeightProportional">
            <ScrollView>
                <StackLayout Padding="20" Spacing="15">
                    <Label Text="Mis Calificaciones"
                           FontSize="30"
                           FontAttributes="Bold"
                           TextColor="{x:Static vm:ThemeColors.PrimaryColor}"
                           HorizontalOptions="Center"
                           Margin="0,20,0,0" />

                    <Label Text="{Binding StudentOverallAverage, StringFormat='Promedio General: {0}'}"
                           FontSize="18"
                           TextColor="{x:Static vm:ThemeColors.PrimaryColor}"
                           HorizontalOptions="Center"
                           Margin="0,20,0,0" />

                    <Picker Title="Selecciona un lapso"
                            ItemsSource="{Binding Lapsos}"
                            SelectedItem="{Binding SelectedLapso}"
                            ItemDisplayBinding="{Binding Nombre}"
                            TextColor="{x:Static vm:ThemeColors.PrimaryColor}"
                            HorizontalOptions="FillAndExpand" />

                    <Label Text="{Binding StudentLapsoAverage, StringFormat='Promedio del lapso: {0}'}"
                           FontSize="18"
                           TextColor="{x:Static vm:ThemeColors.PrimaryColor}"
                           FontAttributes="Bold"
                           HorizontalOptions="Center"
                           Margin="0,0,0,0" />

                    <CollectionView ItemsSource="{Binding GradesByCourse}" Margin="0,10,0,0">
                        <CollectionView.ItemTemplate>
                            <DataTemplate>
                                <StackLayout Spacing="10" Padding="0,5,0,5">
                                    <Frame CornerRadius="10" BackgroundColor="{x:Static vm:ThemeColors.AccentColor}" Padding="15">
                                        <StackLayout>
                                            <Label Text="{Binding Curso}"
                                                   FontAttributes="Bold"
                                                   FontSize="18"
                                                   TextColor="{x:Static vm:ThemeColors.PrimaryColor}" />
                                            <BoxView HeightRequest="1" Color="{x:Static vm:ThemeColors.PrimaryColor}" Margin="0,5" />

                                            <CollectionView ItemsSource="{Binding Calificaciones}">
                                                <CollectionView.ItemTemplate>
                                                    <DataTemplate>
                                                        <Grid Padding="5,4" ColumnSpacing="8" RowSpacing="2">
                                                            <Grid.RowDefinitions>
                                                                <RowDefinition Height="Auto" />
                                                                <RowDefinition Height="Auto" />
                                                            </Grid.RowDefinitions>
                                                            <Grid.ColumnDefinitions>
                                                                <ColumnDefinition Width="*" />
                                                                <ColumnDefinition Width="Auto" />
                                                            </Grid.ColumnDefinitions>

                                                            <Label Grid.Row="0" Grid.Column="0"
                                                                   Text="{Binding Evaluacion}"
                                                                   TextColor="{x:Static vm:ThemeColors.PrimaryColor}" />

                                                            <Label Grid.Row="0" Grid.Column="1"
                                                                   Text="{Binding DisplayGrade}"
                                                                   TextColor="{x:Static vm:ThemeColors.PrimaryColor}"
                                                                   FontAttributes="Bold"
                                                                   HorizontalTextAlignment="End"
                                                                   HorizontalOptions="End" />

                                                            <Label Grid.Row="1" Grid.ColumnSpan="2"
                                                                   Text="{Binding Comments}"
                                                                   TextColor="{x:Static vm:ThemeColors.SecondaryColor}"
                                                                   FontSize="13" />
                                                        </Grid>
                                                    </DataTemplate>
                                                </CollectionView.ItemTemplate>
                                            </CollectionView>
                                        </StackLayout>
                                    </Frame>
                                </StackLayout>
                            </DataTemplate>
                        </CollectionView.ItemTemplate>
                    </CollectionView>
                </StackLayout>
            </ScrollView>
        </ContentView>

        <ContentView x:Name="SideMenu"
                     WidthRequest="260"
                     BackgroundColor="{x:Static vm:ThemeColors.PrimaryColor}"
                     IsVisible="False"
                     TranslationX="-260"
                     AbsoluteLayout.LayoutBounds="0,0,260,1"
                     AbsoluteLayout.LayoutFlags="PositionProportional,HeightProportional">
            <local:MenuPage />
        </ContentView>

        <Grid BackgroundColor="{x:Static vm:ThemeColors.PageBackgroundColor}" Padding="10"
              AbsoluteLayout.LayoutBounds="0,1,1,80"
              AbsoluteLayout.LayoutFlags="WidthProportional,PositionProportional">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <StackLayout Grid.Column="0" HorizontalOptions="Center">
                <Button Text="&#xf015;" FontFamily="FontAwesomeSolid" FontSize="24"
                        TextColor="{x:Static vm:ThemeColors.PrimaryColor}" BackgroundColor="{x:Static vm:ThemeColors.PageBackgroundColor}" Command="{Binding HomeCommand}" />
                <Label Text="Inicio" FontSize="10" HorizontalOptions="Center" />
            </StackLayout>

            <StackLayout Grid.Column="1" HorizontalOptions="Center">
                <Button Text="&#xf007;" FontFamily="FontAwesomeSolid" FontSize="24"
                        TextColor="{x:Static vm:ThemeColors.PrimaryColor}" BackgroundColor="{x:Static vm:ThemeColors.PageBackgroundColor}" Command="{Binding FirstProfileCommand}" />
                <Label Text="Perfil" FontSize="10" HorizontalOptions="Center" />
            </StackLayout>

            <StackLayout Grid.Column="2" HorizontalOptions="Center">
                <Button Text="&#xf0c9;" FontFamily="FontAwesomeSolid" FontSize="24"
                        TextColor="{x:Static vm:ThemeColors.PrimaryColor}" BackgroundColor="{x:Static vm:ThemeColors.PageBackgroundColor}"
                        Clicked="MenuButton_Clicked" />
                <Label Text="Menú" FontSize="10" HorizontalOptions="Center" />
            </StackLayout>
        </Grid>
    </AbsoluteLayout>
</ContentPage>