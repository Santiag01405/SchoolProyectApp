<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="SchoolProyectApp.Views.StudentMenuPage">

    <ContentPage.Content>
        <VerticalStackLayout Padding="20">
            <Label Text="Menú Estudiante" FontSize="24" HorizontalOptions="Center"/>
            <Button Text="Ver Notificaciones" Command="{Binding ViewNotificationsCommand}"/>
            <Button Text="Ver Cursos" Command="{Binding ViewCoursesCommand}"/>
            <Button Text="Ver Calificaciones" Command="{Binding ViewGradesCommand}"/>
        </VerticalStackLayout>
    </ContentPage.Content>
</ContentPage>