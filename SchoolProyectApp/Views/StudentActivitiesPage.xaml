<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:SchoolProyectApp.ViewModels"
             xmlns:views="clr-namespace:SchoolProyectApp.Views"
             xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             xmlns:droid="clr-namespace:CommunityToolkit.Maui.PlatformConfiguration.AndroidSpecific;assembly=CommunityToolkit.Maui"
             x:Class="SchoolProyectApp.Views.StudentActivitiesPage"
             Title="Actividades del Estudiante"
             x:Name="ThisPage"
             droid:NavigationBar.Color="{x:Static local:ThemeColors.PageBackgroundColor}"
             droid:NavigationBar.Style="LightContent"
             BackgroundColor="{x:Static local:ThemeColors.PageBackgroundColor}">

    <ContentPage.BindingContext>
        <local:StudentActivitiesViewModel />
    </ContentPage.BindingContext>

    <ContentPage.Behaviors>
        <toolkit:StatusBarBehavior StatusBarColor="{x:Static local:ThemeColors.PrimaryColor}"
                                   StatusBarStyle="LightContent" />
    </ContentPage.Behaviors>

    <AbsoluteLayout>
        <Grid AbsoluteLayout.LayoutBounds="0,0,1,1" AbsoluteLayout.LayoutFlags="All" RowDefinitions="*,Auto">

            <ScrollView Grid.Row="0">
                <StackLayout Padding="20">
                    <Label Text="Actividades Extracurriculares" FontSize="25" TextColor="{x:Static local:ThemeColors.PrimaryColor}" FontAttributes="Bold" HorizontalOptions="Center" HorizontalTextAlignment="Center" LineBreakMode="TailTruncation" Margin="0,35,0,10"/>
                    <ActivityIndicator IsRunning="{Binding IsBusy}" IsVisible="{Binding IsBusy}" HorizontalOptions="CenterAndExpand" VerticalOptions="CenterAndExpand" Color="{x:Static local:ThemeColors.SecondaryColor}" />
                    <Label Text="{Binding Message}" HorizontalOptions="Center" TextColor="{x:Static local:ThemeColors.DangerColor}" Margin="0,10,0,10"/>
                    <CollectionView ItemsSource="{Binding Activities}">
                        <CollectionView.ItemTemplate>
                            <DataTemplate>
                                <Frame CornerRadius="10" Margin="0,5" Padding="15" BorderColor="LightGray" HasShadow="True">
                                    <StackLayout>
                                        <Label Text="{Binding Name}" FontSize="18" FontAttributes="Bold"/>
                                        <Label Text="{Binding DayOfWeekString}" FontSize="14" FontAttributes="Bold" TextColor="{x:Static local:ThemeColors.PrimaryColor}"/>
                                        <Label Text="{Binding Description}" FontSize="14" TextColor="Gray"/>
                                    </StackLayout>
                                </Frame>
                            </DataTemplate>
                        </CollectionView.ItemTemplate>
                    </CollectionView>
                </StackLayout>
            </ScrollView>

            <Grid Grid.Row="1" BackgroundColor="{x:Static local:ThemeColors.PageBackgroundColor}" Padding="10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <StackLayout Grid.Column="0" HorizontalOptions="Center">
                    <Button Text="&#xf015;" FontFamily="FontAwesomeSolid" FontSize="24"
                            TextColor="{x:Static local:ThemeColors.PrimaryColor}" BackgroundColor="{x:Static local:ThemeColors.PageBackgroundColor}" Command="{Binding HomeCommand}" />
                    <Label Text="Inicio" FontSize="10" HorizontalOptions="Center" TextColor="{x:Static local:ThemeColors.PrimaryColor}"/>
                </StackLayout>

                <StackLayout Grid.Column="1" HorizontalOptions="Center">
                    <Button Text="&#xf007;" FontFamily="FontAwesomeSolid" FontSize="24"
                            TextColor="{x:Static local:ThemeColors.PrimaryColor}" BackgroundColor="{x:Static local:ThemeColors.PageBackgroundColor}" Command="{Binding FirstProfileCommand}" />
                    <Label Text="Perfil" FontSize="10" HorizontalOptions="Center" TextColor="{x:Static local:ThemeColors.PrimaryColor}"/>
                </StackLayout>

                <StackLayout Grid.Column="2" HorizontalOptions="Center">
                    <Button Text="&#xf0c9;" FontFamily="FontAwesomeSolid" FontSize="24"
                            TextColor="{x:Static local:ThemeColors.PrimaryColor}" BackgroundColor="{x:Static local:ThemeColors.PageBackgroundColor}"
                            Clicked="MenuButton_Clicked" />
                    <Label Text="Menú" FontSize="10" HorizontalOptions="Center" TextColor="{x:Static local:ThemeColors.PrimaryColor}"/>
                </StackLayout>
            </Grid>
        </Grid>

        <ContentView x:Name="SideMenu"
                     WidthRequest="260"
                     BackgroundColor="{x:Static local:ThemeColors.PrimaryColor}"
                     IsVisible="False"
                     TranslationX="-260"
                     AbsoluteLayout.LayoutBounds="0,0,260,1"
                     AbsoluteLayout.LayoutFlags="PositionProportional,HeightProportional">
            <views:MenuPage />
        </ContentView>
    </AbsoluteLayout>
</ContentPage>