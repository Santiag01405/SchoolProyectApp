<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="SchoolProyectApp.Views.TeacherMenuPage">

    <ContentPage.Content>
        <VerticalStackLayout Padding="20">
            <Label Text="Menú Maestro" FontSize="24" HorizontalOptions="Center"/>
            <Button Text="Ver Notificaciones" Command="{Binding ViewNotificationsCommand}"/>
            <Button Text="Ver Cursos" Command="{Binding ViewCoursesCommand}"/>
            <Button Text="Ver Estudiantes" Command="{Binding ViewStudentsCommand}"/>
            <Button Text="Asignar Calificaciones" Command="{Binding AssignGradesCommand}"/>
        </VerticalStackLayout>
    </ContentPage.Content>
</ContentPage>
